/* let myCanvas = document.getElementById("graph").getContext("2d");

let chart = new chart(myCanvas, {
    type : "bar",
    data : {
        labels : [],
        datasets : [
            {
                label : "Prioridad"
            }
        ]
    }
}) */